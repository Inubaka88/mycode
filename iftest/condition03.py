#!/usr/bin/evn python3

hostname = input("What value should we set for the hostname?")

if hostname.lower() == "mtg":
    print("The hostname was found to be mtg.\nThe hostname matches expected config.")
    print("Exiting the script")

